%!
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Michael Camara
% Honor Code Pledge: This work is mine unless otherwise cited
% Lab 3: Expressions in Other Languages
% Due: 9/24/15
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% Draw 4 people with different body proportions

/x 0 def
/y 400 def
/legLen 50 def
/armLen 50 def
/bodyLen 200 def
/headLen 100 def

x y moveto % top left head
headLen 0 rlineto
0 headLen neg rlineto
headLen neg 0 rlineto
0 headLen rlineto
headLen 2 div headLen neg rmoveto % move to neck
0 bodyLen neg rlineto % draw body
legLen neg legLen neg rlineto % left leg
legLen legLen rmoveto
legLen legLen neg rlineto % right leg
legLen neg legLen rmoveto 
0 bodyLen 0.5 mul rmoveto % move to arm positions
armLen neg armLen rlineto % left arm
armLen armLen neg rmoveto
armLen armLen rlineto % right arm

/x x 200 add def
/y y 100 add def
/legLen legLen 20 add def
/armLen armLen 20 add def
/bodyLen bodyLen 20 sub def
/headLen headLen 50 sub def

x y moveto % top left head
headLen 0 rlineto
0 headLen neg rlineto
headLen neg 0 rlineto
0 headLen rlineto
headLen 2 div headLen neg rmoveto % move to neck
0 bodyLen neg rlineto % draw body
legLen neg legLen neg rlineto % left leg
legLen legLen rmoveto
legLen legLen neg rlineto % right leg
legLen neg legLen rmoveto 
0 bodyLen 0.5 mul rmoveto % move to arm positions
armLen neg armLen rlineto % left arm
armLen armLen neg rmoveto
armLen armLen rlineto % right arm

/x x 200 add def
/y y 100 sub def
/legLen legLen 35 sub def
/armLen armLen 35 sub def
/bodyLen bodyLen 20 add def
/headLen headLen 20 add def

x y moveto % top left head
headLen 0 rlineto
0 headLen neg rlineto
headLen neg 0 rlineto
0 headLen rlineto
headLen 2 div headLen neg rmoveto % move to neck
0 bodyLen neg rlineto % draw body
legLen neg legLen neg rlineto % left leg
legLen legLen rmoveto
legLen legLen neg rlineto % right leg
legLen neg legLen rmoveto 
0 bodyLen 0.5 mul rmoveto % move to arm positions
armLen neg armLen rlineto % left arm
armLen armLen neg rmoveto
armLen armLen rlineto % right arm

/x x 100 sub def
/y y 350 add def
/legLen legLen 35 add def
/armLen armLen 35 add def
/bodyLen bodyLen 50 sub def
/headLen headLen 20 add def

x y moveto % top left head
headLen 0 rlineto
0 headLen neg rlineto
headLen neg 0 rlineto
0 headLen rlineto
headLen 2 div headLen neg rmoveto % move to neck
0 bodyLen neg rlineto % draw body
legLen neg legLen neg rlineto % left leg
legLen legLen rmoveto
legLen legLen neg rlineto % right leg
legLen neg legLen rmoveto 
0 bodyLen 0.5 mul rmoveto % move to arm positions
armLen neg armLen rlineto % left arm
armLen armLen neg rmoveto
armLen armLen rlineto % right arm

stroke
showpage
